#ifndef INVALID_NUMBER_INPUT_MESSAGE_HPP
#define INVALID_NUMBER_INPUT_MESSAGE_HPP

#include <string>

using std::string;

/**
 * @brief Message for when the user does not enter a string of integers
 */
string InvalidNumberInputMessage();

#endif